# Workshop assets

- `preview.png` — cropped screenshot for the Workshop preview;
- `modicon_source.png` — cropped screenshot source for compiling `modicon.tex`/`modicon.xml` with Don't Starve Mod Tools.

These files are not a custom model. The model remains the current built-in DST model until a separate model contribution is approved.
